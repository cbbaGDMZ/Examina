name: examin
description: "Corrector Óptico de Exámenes"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_mlkit_document_scanner: ^0.2.0
  camera: ^0.10.5+5
  image: ^4.1.3
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  path: ^1.8.3
  flutter_riverpod: ^2.4.9
  intl: ^0.19.0
  file_picker: ^8.0.0
  pdfrx: ^1.0.60

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
